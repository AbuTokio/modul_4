import type { Product } from "../interfaces/Product"

export async function getProducts_store(): Promise<Product[]> {
  // const {data: products, error }

  return [] // TODO: Fetch products from Supabase
}
